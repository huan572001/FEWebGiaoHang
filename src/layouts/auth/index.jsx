import React, { useEffect, useRef, useCallback } from 'react';
import './index.less';

const Layout = ({ children }) => {
  return (
    <>
      <div>{children}</div>
    </>
  );
};
export default Layout;
